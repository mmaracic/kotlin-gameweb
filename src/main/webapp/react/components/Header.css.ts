import { css } from '@emotion/react'
import { green, grey } from '@mui/material/colors';

export const backgroundLightGrayStyle = css({
    backgroundColor: grey[300]
});

export const greenStyle = css({
    '.greenColor': {
        color: green[500]
    }
});